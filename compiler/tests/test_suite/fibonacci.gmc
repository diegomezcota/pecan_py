program fibonacci;

function fib(int n) returns int {
    var int ans;
    if (n < 2) {
        ans = n;
    } else {
        ans = fib(n - 1) + fib(n - 2);
    }
    return ans;
}

main() {
    var int i;
    for i = 0 to 10 {
        write(fib(i));
    }
}