// Test 4: Dedicado a usar todas las estructuras de flujo de control
program flujos_de_control;

main() {
    var string hola;
    var int a;
    var bool b;
    var float c;
    if (a && c) {
        write(a, b, 3 * 10);
    } else {
        c = 2.456 * a + 45;
        write(10 * a);
    }

    if (1 + 3 + 5 * 10 && c) {
        hola = "que paso";
    }
}