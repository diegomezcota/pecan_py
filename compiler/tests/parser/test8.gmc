program a;

class Animal {
    var string nombre;
    var int edad;
    var string tipo;
};

constructor Animal(string nombre, int edad) {
    $nombre = nombre;
    $edad = edad;
    $tipo = "trips";
    write("tripsi");
}

@class Animal function constructor_fake(string nombre, int edad, string tipo) returns void {
    $nombre = nombre;
    $edad = edad;
    $tipo = tipo;
    return;
}

// hay que bloquear la definicion del mismo tipo en un metodo de la clase
@class Animal function set_nombre(string nombre) returns void {
    write("Cambiando nombre...", "Viejo nombre:", $nombre, "Nuevo nombre:", nombre);
    $nombre = nombre;
    return;
}

@class Animal function get_nombre() returns string {
    return $nombre;
}

@class Animal function comer(int iTimes) returns void {
    var int i;
    for i = 0 to iTimes {
        write(i + 1, $nombre, "esta comiendo...");
    }
    return;
}

obj shori = Animal();
obj trips = Animal();

main() {
    obj brunis = Animal();
    obj lucas = Animal();
    //shori.constructor_fake("Shori Mtz Celis", 5, "perro");
    brunis.constructor_fake("Bruno Gomez Cota", 8, "perro");
    brunis.set_nombre("Calypso");
    write(brunis.get_nombre());
    //write(brunis.get_nombre());
}